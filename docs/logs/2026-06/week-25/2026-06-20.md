# 2026-06-20

## 10:00 KST

### HUP-2 minimal apps initialization

- Created the initial `apps/web` React Router application.
- Created the initial `apps/api` FastAPI application.
- Created the initial `apps/worker` Python worker application.
- Added package-level scripts so Turborepo can run workspace tasks.
- Added uv-managed Python project files for the API and worker.
- Updated the lockfile after adding the initial application dependencies.

Linear completion source: HUP-2 completed at `2026-06-20T01:15:53.200Z`.

### HUP-3 bootstrap README and ignore rules

- Expanded the root README with project bootstrap instructions.
- Documented how a new developer can install dependencies and run the project.
- Added `.nvmrc` to pin the expected Node.js version.
- Improved `.gitignore` coverage for local development artifacts.
- Adjusted API dev scripts toward a stable Uvicorn-based workflow.

Linear completion source: HUP-3 completed at `2026-06-20T01:28:48.657Z`.

## 16:00 KST

### HUP-4 PostgreSQL and pgvector Docker Compose setup

- Added Docker Compose configuration for local PostgreSQL with pgvector.
- Added the `huposit-postgres` service using the `pgvector/pgvector:pg17` image.
- Configured the local database name, user, password, port mapping, volume, and healthcheck.
- Added a PostgreSQL init script to enable the `vector` extension.
- Added a root `db:up` script for starting the local database.
- Updated the README with local database startup guidance.

Linear completion source: HUP-4 completed at `2026-06-20T06:33:07.095Z`.

## 18:00 KST

### HUP-5 FastAPI database connection setup

- Added `.env.example` with the PostgreSQL async database URL.
- Added API settings loading through `pydantic-settings`.
- Added SQLAlchemy async engine and session factory configuration.
- Added FastAPI dependency wiring for async database sessions.
- Added `/health/db` to verify PostgreSQL connectivity with `SELECT 1`.
- Added database error handling that returns service-unavailable responses when the database is unreachable.
- Added API dependencies for SQLAlchemy, asyncpg, and pydantic-settings.

Linear completion source: HUP-5 completed at `2026-06-20T08:57:21.350Z`.

## 20:00 KST

### HUP-6 Linear, Codex, GitHub PR workflow documentation

- Added `docs/workflow.md` as the repository source of truth for Linear issue, branch, commit, PR, merge, and cleanup rules.
- Documented the issue drafting flow around clear `Goal`, `Scope`, and `Done when` sections.
- Defined branch naming, Korean commit message format, PR title/body expectations, squash merge, and branch cleanup rules.
- Added Codex operating rules for reading issues, checking status, staying scoped, reporting verification, and requiring explicit commit or push approval.
- Connected the README, AGENTS instructions, and initial development logs to the workflow.

Linear completion source: HUP-6 completed at `2026-06-20T10:31:28.636Z`.
